export interface TableListItem {
  // title: string;
  key: string;
  name: string;
  icon: string;
  menuType: number;
  sortNo: number;
};

export interface Permission {
  title?: string;
  key?: string;
  phone?: string;
}
